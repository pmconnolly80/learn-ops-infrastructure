# System Map (AI)

## 1. System Diagram


┌─────────────────┐                                    ┌─────────────────┐
│   GitHub API    │                                    │    Slack API    │
│ api.github.com  │                                    │ slack.com/api   │
└────────┬────────┘                                    └────────┬────────┘
         │ HTTPS POST/PUT (Bearer token)                        │ HTTPS POST (Bot token)
         │                                                      │
         │              ┌───────────────────────────────────────┘
         │              │
         ▼              ▼
┌────────────────────────────────────┐
│          Django REST API           │
│       Python 3.11 / DRF            │
│            :8000                   │
│         (debug: 5678)              │
└──┬──────────┬──────────────────────┘
   │          │                    ▲
   │          │                    │ HTTP REST
   │          │                    │
   │          │          ┌─────────┴────────┐
   │          │          │   React Client   │
   │          │          │   React 16.13    │
   │          │          │      :3000       │
   │          │          └──────────────────┘
   │          │
   │ TCP      │ Valkey protocol
   │ :5432    │ :6379
   │          │
   ▼          ▼
┌──────────────────┐     ┌──────────────────┐
│   PostgreSQL 16  │     │     Valkey        │
│      :5432       │     │  (Redis-compat)   │
└────────┬─────────┘     │      :6379        │
         │               └──────────────────┘
         │ psql :5432
         ▼
┌──────────────────┐
│ Postgres         │
│ Exporter         │       ┌──────────────────┐
│    :9187         │       │   Django API     │
└────────┬─────────┘       │ /metrics/metrics │
         │ HTTP scrape      │     :8000        │
         │                  └────────┬─────────┘
         ▼                           │ HTTP scrape
┌────────────────────────────────────┘
│
▼
┌──────────────────┐
│   Prometheus     │
│    :9090         │
└────────┬─────────┘
         │ HTTP query
         ▼
┌──────────────────┐
│    Grafana       │
│    :3001         │
└──────────────────┘