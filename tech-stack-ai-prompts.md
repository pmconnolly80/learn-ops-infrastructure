# Tech Stack (AI)

## 1. Run Questions

### 1a. Config Files

Find all config and environment files in this repo and fill in at least 3 key config values from each. Use only the config variable name, not its value (e.g. write `POSTGRES_USER`, not `POSTGRES_USER=supersecretpassword123`):


### 1b. How to Start It

Find the Makefile at the root of this repo and list the targets relevant for starting the system. Explain what each target does and how they differ:

### 1c. Where to Access It

Fill in the table using the config files and docker-compose files in this repo:

### 1d. Service Dependencies

For each service, identify what other services it depends on and explain why that dependency exists:

### 1e. Main Entry Points

For each service, find the file where the process starts AND the separate file where incoming requests are routed. These are two different files:


---

## 2. Services

Fill in the table with the services defined in this repo:


---

## 3. System Overview

Write 3 paragraphs. In the first, describe what kind of application this is and what problem it solves. In the second, describe its main features from the perspective of someone using it. In the third, describe who uses it and how - are there different roles, and do they interact with the system differently?
